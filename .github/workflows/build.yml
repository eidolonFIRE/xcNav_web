# TODO: Missing Licence Header

name: builds

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  npx-webpack:
    strategy:
      matrix:
        os: [ubuntu-18.04, macos-10.15]

    name: npx-webpack-on-${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    steps:
    - name: checkout the app
      uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - name: compile client
      run: |
        cd client
        npm run setup
        npm run compile

    - name: compile server
      run: |
        cd server
        npm run setup
        npm run compiles
